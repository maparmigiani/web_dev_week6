<!--
*Name: Maiara Almeida
*Student id: 301145511
--> 

<!--Defining subpages to the website and how the header works--> 
<nav class="navbar navbar-expand-lg navbar-light bg-primary">                
    <div class="container-fluid">
      <!--Include a image to the header--> 
      <img src="/Assets/images/logo.png" width="40" height="50" class="d-inline-block align-top" alt="">
      <!--Define a general name to the header--> 
      <a class="navbar-brand" href="/home">Maiara Almeida</a>
      
      <!--Define the subpages--> 
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if(title == "Home"){ %>
            <li class="nav-link active">
            <%} else {%>
            <li class="nav-item">
            <% } %>
            <a class="nav-link" aria-current="page" href="/home"><i class="fas fa-house-user"></i> Home</a>
          </li>
  
          <% if(title == "Service"){ %>
            <li class="nav-link active">
            <%} else {%>
            <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/services"><i class="fas fa-laptop-code"></i> Services</a>
          </li>
  
          <% if(title == "About"){ %>
            <li class="nav-link active">
            <%} else {%>
            <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/about"> <i class="fas fa-user-friends"></i> About</a>
          </li>
  
          <% if(title == "Projects"){ %>
            <li class="nav-link active">
            <%} else {%>
            <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/projects"><i class="fas fa-bars"></i> Projects</a>
          </li>
  
          <% if(title == "Contact"){ %>
            <li class="nav-link active">
            <%} else {%>
            <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/contact"><i class="far fa-envelope"></i> Personal Contact</a>
          </li>

          <% if(title == "Bussiness Information Contact"){ %>
            <li class="nav-link active">
            <%} else {%>
            <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/information-list"><i class="far fa-solid fa-address-card"></i> Bussiness Information Contact</a>
          </li>

          <% if(typeof displayName == 'string' && displayName) { %>
            <li class="navbar-text"><i class="fas fa-lg fa-user"></i> Welcome, <%= displayName %></li>
            <li class="nav-item"><a href="/logout" class="nav-link">
              <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
          <% } else { %>
            <li class="nav-item"><a href="/login" class="nav-link">
              <i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
          <% } %>
          
        </ul>
       
      </div>
    </div>

 </nav>

<!--Including an introduction message in all pages--> 
<main class= "container">
  <div class="jumbotron">
    <h1><p>Welcome to the  <%= title %> page</p></h1>

</main>

 
 